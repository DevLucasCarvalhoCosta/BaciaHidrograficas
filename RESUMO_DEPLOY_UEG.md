# 🎯 Resumo Visual - Deploy Servidor UEG

## 📊 Arquitetura de Deploy

```
┌─────────────────────────────────────────────────────────────────┐
│                    SERVIDOR UEG (Ubuntu 24.04)                  │
│                    200.137.241.42:8740                           │
└─────────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌──────────────┐      ┌──────────────┐     ┌──────────────┐
│    Nginx     │      │     PM2      │     │ PostgreSQL   │
│   (Port 80)  │      │              │     │  (Port 5432) │
│  (Port 443)  │      │              │     │              │
└──────────────┘      └──────────────┘     └──────────────┘
        │                     │                     │
        │            ┌────────┴────────┐            │
        │            │                 │            │
        │            ▼                 ▼            │
        │     ┌─────────────┐   ┌──────────┐       │
        │     │ patrimonio  │   │   ana    │       │
        │     │  -backend   │   │ -backend │       │
        │     │  (Port 3000)│   │(Port 3001)│      │
        │     └─────────────┘   └──────────┘       │
        │                               │           │
        ├───────────────────────────────┴───────────┤
        │                                           │
┌───────┴──────┐                          ┌─────────┴──────┐
│  /           │                          │  ana_hidro     │
│ patrimonio   │                          │  (database)    │
│  -frontend   │                          │                │
└──────────────┘                          └────────────────┘
        │
┌───────┴──────┐
│    /ana      │
│ ana-frontend │
└──────────────┘
```

## 🌐 Mapeamento de URLs

```
https://patrimonioueg.duckdns.org
├── /                          → PatrimônioUEG Frontend (EXISTENTE)
├── /api                       → PatrimônioUEG Backend (EXISTENTE)
├── /n8n                       → n8n Automação (EXISTENTE)
│
└── /ana/                      → 🆕 ANA Frontend (NOVO)
    └── /api/ana/              → 🆕 ANA Backend (NOVO)
        ├── /health            → Health check
        ├── /estacoes/hidro    → Lista estações
        └── /bacias            → Lista bacias
```

## 🔄 Fluxo de Deploy (GitHub Actions)

```
┌─────────────────────┐
│   Push to GitHub    │
│   (branch: main)    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  GitHub Actions     │
│  Workflow Triggered │
└──────────┬──────────┘
           │
     ┌─────┴─────┐
     │   JOBS    │
     └─────┬─────┘
           │
    ┌──────┴──────────────────────┐
    │                             │
    ▼                             ▼
┌────────────┐           ┌─────────────────┐
│ Pre-Check  │           │     Build       │
│            │           │                 │
│ ✓ SSH      │           │ ✓ Backend       │
│ ✓ Disk     │           │ ✓ Frontend      │
│ ✓ PM2      │           │ ✓ Dependencies  │
└─────┬──────┘           └────────┬────────┘
      │                           │
      └───────────┬───────────────┘
                  │
                  ▼
         ┌─────────────────┐
         │     Deploy      │
         │                 │
         │ 1. Stop PM2     │
         │ 2. Transfer     │
         │ 3. Configure    │
         │ 4. DB Migrate   │
         │ 5. Start PM2    │
         │ 6. Reload Nginx │
         └────────┬────────┘
                  │
                  ▼
         ┌─────────────────┐
         │  Health Check   │
         │                 │
         │ ✓ Backend API   │
         │ ✓ Frontend      │
         │ ✓ Patrimônio OK │
         └─────────────────┘
```

## 📁 Estrutura de Arquivos no Servidor

```
Servidor UEG
│
├── /home/usuario/
│   ├── ana-backend/                    🆕 NOVO
│   │   ├── dist/                       # Código compilado
│   │   ├── node_modules/               # Dependências
│   │   ├── prisma/                     # Schema do banco
│   │   ├── .env                        # Variáveis de ambiente
│   │   ├── ecosystem.config.js         # Config PM2
│   │   └── package.json
│   │
│   ├── patrimonio-backend/             ✅ EXISTENTE (não mexer)
│   │
│   ├── logs/
│   │   ├── ana-backend-out.log         🆕 NOVO
│   │   └── ana-backend-error.log       🆕 NOVO
│   │
│   └── backups/
│       └── pre-ana-deploy/             🆕 Backup antes do deploy
│
├── /var/www/
│   ├── ana-frontend/                   🆕 NOVO
│   │   └── dist/                       # Build do Vite
│   │       ├── index.html
│   │       ├── assets/
│   │       └── ...
│   │
│   └── patrimonio-frontend/            ✅ EXISTENTE (não mexer)
│
└── /etc/nginx/
    └── sites-available/
        └── patrimonioueg               ✏️ MODIFICADO (adicionar config ANA)
```

## 🔐 Secrets do GitHub (Obrigatórios)

```
┌─────────────────────────────────────────────────────┐
│  GitHub Repository Settings → Secrets → Actions     │
├─────────────────────────────────────────────────────┤
│                                                     │
│  UEG_SSH_HOST       = 200.137.241.42                │
│  UEG_SSH_PORT       = 8740                          │
│  UEG_SSH_USER       = usuario                       │
│  UEG_SSH_KEY        = [chave privada SSH]           │
│  DATABASE_URL       = postgresql://...              │
│  ANA_BASE_URL       = https://dadosabertos.ana...   │
│                                                     │
└─────────────────────────────────────────────────────┘
```

## ⚡ Checklist Rápido

### Antes do Primeiro Deploy

```
┌─────────────────────────────────────────────┐
│  NO SERVIDOR                                │
├─────────────────────────────────────────────┤
│  ☐ Executar verificacao_servidor.sh         │
│  ☐ Criar banco ana_hidro                    │
│  ☐ Adicionar config no Nginx                │
│  ☐ Testar: sudo nginx -t                    │
│  ☐ Gerar deploy keys SSH                    │
│  ☐ Salvar backup do estado atual            │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  NO GITHUB                                  │
├─────────────────────────────────────────────┤
│  ☐ Adicionar Deploy Key                     │
│  ☐ Adicionar Secrets (6 secrets)            │
│  ☐ Verificar workflow .yml existe           │
└─────────────────────────────────────────────┘
```

### Durante o Deploy

```
┌─────────────────────────────────────────────┐
│  1. Push para main                          │
│  2. Acompanhar Actions no GitHub            │
│  3. Monitorar logs: pm2 logs ana-backend    │
│  4. Verificar health check                  │
└─────────────────────────────────────────────┘
```

### Após o Deploy

```
┌─────────────────────────────────────────────┐
│  TESTAR                                     │
├─────────────────────────────────────────────┤
│  ✓ Frontend ANA      /ana                   │
│  ✓ Backend ANA       /api/ana/health        │
│  ✓ PatrimônioUEG     /                      │
│  ✓ API Patrimônio    /api                   │
│  ✓ n8n               /n8n                   │
├─────────────────────────────────────────────┤
│  VERIFICAR                                  │
├─────────────────────────────────────────────┤
│  ✓ pm2 list (ambos online)                  │
│  ✓ sudo nginx -t (OK)                       │
│  ✓ Logs sem erros                           │
└─────────────────────────────────────────────┘
```

## 🚨 Troubleshooting Rápido

### Backend não inicia
```bash
pm2 logs ana-backend --err --lines 50
pm2 restart ana-backend
```

### 502 Bad Gateway
```bash
pm2 list
sudo nginx -t
sudo tail -n 50 /var/log/nginx/error.log
```

### Frontend não carrega
```bash
ls -la /var/www/ana-frontend/dist/
sudo chown -R www-data:www-data /var/www/ana-frontend
```

### Porta 3001 em uso
```bash
sudo lsof -i :3001
pm2 list
```

## 📊 Comparação: Antes vs Depois

### ANTES (Só PatrimônioUEG)
```
PM2 Processes:
┌─────┬────────────────────┬────────┬────────┐
│ id  │ name               │ status │ port   │
├─────┼────────────────────┼────────┼────────┤
│ 0   │ patrimonio-backend │ online │ 3000   │
└─────┴────────────────────┴────────┴────────┘

URLs:
/ ................. PatrimônioUEG
/api .............. PatrimônioUEG API
/n8n .............. n8n
```

### DEPOIS (PatrimônioUEG + ANA) 🆕
```
PM2 Processes:
┌─────┬────────────────────┬────────┬────────┐
│ id  │ name               │ status │ port   │
├─────┼────────────────────┼────────┼────────┤
│ 0   │ patrimonio-backend │ online │ 3000   │
│ 1   │ ana-backend        │ online │ 3001   │ 🆕
└─────┴────────────────────┴────────┴────────┘

URLs:
/ ................. PatrimônioUEG
/api .............. PatrimônioUEG API
/n8n .............. n8n
/ana .............. ANA Frontend 🆕
/api/ana .......... ANA Backend 🆕
```

## 🎯 Comandos Essenciais

```bash
# Conectar
ssh -p 8740 usuario@200.137.241.42

# Monitorar
pm2 logs ana-backend
pm2 monit

# Status
pm2 list
sudo nginx -t

# Restart
pm2 restart ana-backend
sudo systemctl reload nginx

# Logs
sudo tail -f /var/log/nginx/error.log
```

## 📞 Suporte

**Guias Completos:**
- 📘 [GUIA_DEPLOY_COMPLETO.md](GUIA_DEPLOY_COMPLETO.md)
- 🔍 [VERIFICACAO_SERVIDOR_UEG.md](VERIFICACAO_SERVIDOR_UEG.md)
- ⚙️ [CONFIGURACAO_UEG.md](CONFIGURACAO_UEG.md)

**Em caso de problemas:**
1. Verificar logs: `pm2 logs ana-backend`
2. Verificar Nginx: `sudo nginx -t`
3. Consultar troubleshooting nos guias
4. Rollback se necessário

---

**✅ Sistema pronto para deploy seguro no servidor UEG!**
